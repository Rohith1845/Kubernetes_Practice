apiVersion: v1
kind: Pod
metadata:
  name: multi-container
  namespace: roboshop
  labels: 
    purpose: multi-container
    project: roboshop
spec:
  containers:
  - name: nginx
    image: nginx:latest
  - name: mongo
    image: mongo:latest